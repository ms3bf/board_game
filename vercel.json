{
  "version": 2,
  "functions": {
    "api/index.py": {
      "excludeFiles": "{data/**,gui/**,*.parquet,test.parquet,demo_trade.parquet,demo_trade.chart.parquet,build_*.py,market_session.py}"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
